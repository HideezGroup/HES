@page "/invite"
@model HES.Web.Pages.Identity.InviteModel
@{
    ViewData["Title"] = Resource.Identity_Invite_Title;
}

<div class="row h-100 justify-content-center align-items-center">
    <div class="custom-card">
        <h1 class="text-navyblue">@Resource.Identity_Invite_Header</h1>
        @if (Model.ErrorMessage == null)
        {
            <form method="post">              
                <input asp-for="Input.Code" type="hidden" />
                <div class="form-group">
                    <label class="text-gray" asp-for="Input.Email"></label>
                    <input asp-for="Input.Email" class="form-control" readonly />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="text-gray" asp-for="Input.FirstName"></label>
                    <input asp-for="Input.FirstName" class="form-control" />
                    <span asp-validation-for="Input.FirstName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="text-gray" asp-for="Input.LastName"></label>
                    <input asp-for="Input.LastName" class="form-control" />
                    <span asp-validation-for="Input.LastName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="text-gray" asp-for="Input.Password"></label>
                    <input asp-for="Input.Password" class="form-control" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="text-gray" asp-for="Input.ConfirmPassword"></label>
                    <input asp-for="Input.ConfirmPassword" class="form-control" />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                </div>
                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">@Resource.Btn_Confirm</button>
                </div>
            </form>
        }
        else
        {
            <p class="text-danger">@Model.ErrorMessage</p>
        }
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}