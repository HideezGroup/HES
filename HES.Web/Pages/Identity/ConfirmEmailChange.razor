@page "/confirm-email-change"
@layout BlankLayout
@inherits HESPageBase

@if (Initialized)
{
    if (Success)
    {
        <div class="d-flex justify-content-center align-items-center vh-100">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h3 class="mb-4">@Resource.Identity_ConfirmEmailChange_Header</h3>
                    <p class="mb-4 text-muted">@Resource.Identity_ConfirmEmailChange_Description</p>
                    <div class="text-center">
                        <a href="@Routes.Login" class="btn btn-outline-primary">@Resource.Btn_Login</a>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <Error Title="@ErrorTitle" Description="@ErrorDescription"/>
    }
}
else if (LoadFailed)
{
    <LoadingFailed ErrorMessage="@ErrorMessage" />
}
else
{
    <Loader AbsoluteCenter="true" />
}