@inherits HESComponentBase

@if (Account == null)
{
    <LoadingText />
}
else
{
    if (EntityBeingEdited)
    {
        <EditedByAnother EntityName="company" CloseAction="ModalDialogService.CancelAsync" />
    }
    else
    {
        <h5 class="text-gray mb-4 text-break">You want to delete @Account.AccountType.ToString().ToLower() account @Account.Name?</h5>
        <div class="text-end">
            <button type="button" class="btn btn-secondary me-1" @onclick="ModalDialogService.CancelAsync">Cancel</button>
            <ButtonSpinner Text="Delete" Class="btn btn-danger" Callback="DeleteAccoountAsync" />
        </div>
    }
}